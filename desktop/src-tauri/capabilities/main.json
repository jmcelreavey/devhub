{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimal surface for the main window. The dashboard is a remote localhost page, so anything granted here is granted to a web origin.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "core:event:allow-listen",
    "dialog:allow-open",
    "opener:allow-open-url",
    "updater:default"
  ]
}
